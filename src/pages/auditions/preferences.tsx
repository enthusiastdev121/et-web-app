import Layout from "components/Layout";
import CastingPreferences from "containers/CastingPreferences";

export default function PreferencesPage() {
  return (
    <Layout>
      <CastingPreferences />
    </Layout>
  );
}
