import Layout from "components/Layout";
import AgreementPage from "components/ProductPage/About/AgreementPage";

export default function Agreement() {
    return (
        <Layout feature={false}>
            <AgreementPage />
        </Layout>
    );
}
